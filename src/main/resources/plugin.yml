name: Lockbar
version: '${project.version}'
main: com.emicb.lockbar.Lockbar
api-version: 1.18
prefix: LockBar
author: Emi Brown
description: '${project.description}'
commands:
  lock:
    description: 'Locks all inventory slots.'
    usage: '/lock'