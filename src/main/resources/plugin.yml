name: Lockbar
version: '${project.version}'
main: com.emicb.lockbar.Lockbar
api-version: 1.18
prefix: LockBar
author: Emi Brown
description: '${project.description}'
commands:
  lock-all:
    description: 'Globally locks all inventories.'
    usage: '/lock-all'
  lock-bar:
    description: 'Globally locks all hotbar slots.'
    usage: '/lock-bar'